'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function AdminLogin(){const [form,setForm]=useState({username:'',password:''});const [error,setError]=useState('');const router=useRouter();async function submit(e){e.preventDefault();setError('');const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});if(res.ok)router.push('/admin/dashboard');else setError((await res.json()).error||'Login failed')}return <main className="hero-bg grid min-h-screen place-items-center px-4"><form onSubmit={submit} className="glass relative z-10 w-full max-w-md rounded-[2rem] p-8"><p className="text-sm uppercase tracking-[.4em] text-[#C9A84C]">Admin</p><h1 className="mt-2 text-5xl">Login</h1><input className="input mt-6" placeholder="Username" onChange={e=>setForm({...form,username:e.target.value})}/><input type="password" className="input mt-4" placeholder="Password" onChange={e=>setForm({...form,password:e.target.value})}/>{error&&<p className="mt-3 text-red-300">{error}</p>}<button className="btn-gold mt-5 w-full rounded-2xl py-3">Login</button></form></main>}
