'use client';
import {useEffect,useState} from 'react';import {motion,AnimatePresence} from 'framer-motion';import {X,ChevronLeft} from 'lucide-react';
const events=['Wedding','Birthday','Other Events','Outdoor Photoshoot','Other Photo Services'];
const weddings=['Hindu Wedding','Christian Wedding','Muslim Wedding','Other Wedding Types'];
const budgets=['₹15,000','₹20,000','₹25,000','₹50,000','Custom Budget'];
export default function QuotePopup(){const [open,setOpen]=useState(false);const [step,setStep]=useState(1);const [loading,setLoading]=useState(false);const [success,setSuccess]=useState('');const [form,setForm]=useState({eventCategory:'',weddingType:'',priceCategory:'',customBudget:'',name:'',mobile:'',email:''});useEffect(()=>{const t=setTimeout(()=>setOpen(true),400);return()=>clearTimeout(t)},[]);function choose(k,v){setForm(f=>({...f,[k]:v})); if(k==='eventCategory') setStep(v==='Wedding'?2:3); if(k==='weddingType') setStep(3); if(k==='priceCategory') setStep(4)}async function submit(e){e.preventDefault();setSuccess(''); if(!form.name||!form.mobile||!form.email) return alert('Please fill all required fields');setLoading(true);const res=await fetch('/api/enquiries',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});setLoading(false);if(res.ok){setSuccess('Thank you! We will contact you soon.');setTimeout(()=>setOpen(false),1400)}else alert((await res.json()).error||'Something went wrong')}return <AnimatePresence>{open&&<motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} className="fixed inset-0 z-[100] grid place-items-center bg-black/80 px-4 backdrop-blur-md"><motion.div initial={{scale:.9,y:25,opacity:0}} animate={{scale:1,y:0,opacity:1}} exit={{scale:.95,opacity:0}} className="glass relative max-h-[92vh] w-full max-w-xl overflow-y-auto rounded-[2rem] p-6 sm:p-8"><button onClick={()=>setOpen(false)} className="absolute right-4 top-4 rounded-full bg-white/10 p-2"><X size={18}/></button>{step>1&&<button onClick={()=>setStep(step-1)} className="mb-4 flex items-center gap-1 text-sm text-[#E8C97A]"><ChevronLeft size={16}/>Back</button>}<p className="text-sm uppercase tracking-[.35em] text-[#C9A84C]">Get Quote</p><h2 className="lux-title mt-2 text-4xl">Plan your event</h2>{success&&<p className="mt-4 rounded-2xl bg-green-500/15 p-3 text-green-200">{success}</p>}{step===1&&<div className="mt-6 grid gap-3">{events.map(x=><button key={x} onClick={()=>choose('eventCategory',x)} className="rounded-2xl border border-white/10 bg-white/5 p-4 text-left hover:border-[#C9A84C]">{x}</button>)}</div>}{step===2&&<div className="mt-6 grid gap-3">{weddings.map(x=><button key={x} onClick={()=>choose('weddingType',x)} className="rounded-2xl border border-white/10 bg-white/5 p-4 text-left hover:border-[#C9A84C]">{x}</button>)}</div>}{step===3&&<div className="mt-6 grid gap-3 sm:grid-cols-2">{budgets.map(x=><button key={x} onClick={()=>choose('priceCategory',x)} className="rounded-2xl border border-white/10 bg-white/5 p-4 text-left hover:border-[#C9A84C]">{x}</button>)}</div>}{step===4&&<form onSubmit={submit} className="mt-6 space-y-4">{form.priceCategory==='Custom Budget'&&<input className="input" placeholder="Custom Budget" value={form.customBudget} onChange={e=>setForm({...form,customBudget:e.target.value})}/>}<input className="input" placeholder="Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input className="input" placeholder="Mobile Number" value={form.mobile} onChange={e=>setForm({...form,mobile:e.target.value})}/><input className="input" placeholder="Email Address" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><button disabled={loading} className="btn-gold w-full rounded-2xl py-3">{loading?'Saving...':'Get Quote'}</button></form>}</motion.div></motion.div>}</AnimatePresence>}
